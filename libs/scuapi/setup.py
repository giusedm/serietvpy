from setuptools import setup, find_packages

setup(
    name='scuapi',
    version='1.0.0',
    description='Libreria scuapi modificata per il progetto Flask',
    author='Il Tuo Nome',
    author_email='tuo.email@example.com',
    packages=find_packages(),
    install_requires=[
        # Elenca qui le dipendenze della tua libreria, se ce ne sono
    ],
)
